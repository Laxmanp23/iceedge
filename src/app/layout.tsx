import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'IceEdge Refrigeration | Cold Storage Solutions',
  description: 'Advanced Cold Storage & Refrigeration Solutions. Specialists in design, supply, installation, and maintenance of cold storage systems.',
  keywords: 'Cold Storage, Refrigeration, Industrial Refrigeration, Chillers, Freezers, AMC, IceEdge',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
