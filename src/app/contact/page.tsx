export const metadata = {
  title: 'Contact Us | IceEdge Refrigeration',
  description: 'Get in touch with IceEdge Refrigeration for inquiries, quotes, or support regarding our cold storage solutions.',
};

import ContactClient from './ContactClient';

export default function ContactPage() {
  return <ContactClient />;
}
